[package]
name = "logly"
version = "0.1.0"
description = "Rust Powered Python Logging Utility"
edition = "2021"
license = "MIT"
readme = "README.md"

[lib]
name = "_logly"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.25.1", features = ["extension-module"] }
